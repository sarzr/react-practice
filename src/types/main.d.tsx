export type props = {
  type: "submit" | "reset" | "button";
  text: string;
  showSvg?: boolean;
  className?: string;
};
export type ComuProps = {
  img: string;
  img_title: string;
  title: string;
  text: string;
  width: string;
};
export type AchieveCardProps = {
  img: string;
  img_title: string;
  count: string;
  title: string;
};
